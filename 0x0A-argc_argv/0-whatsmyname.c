#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: integer
 * @argv: string
 * Return: always 0
 */
int main(int argc, char *argv[])
{
		if (argc > 0)
		printf("Program name: %s\n", argv[0]);
		return (0);
}
